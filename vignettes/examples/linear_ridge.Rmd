### Linear regression

A simple, one-variable Bayesian linear regression model using a *ridge* prior.

```{r linear_greta}
# variables & priors
int  <- variable()
sd   <- cauchy(0, 3, truncation=c(0, Inf))

tau  <- inverse_gamma(1, 1)
coef <- normal(0, tau)

# linear predictor
mu <- int + coef * attitude$complaints

# observation model
distribution(attitude$rating) <- normal(mu, sd)
```
