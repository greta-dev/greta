% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reinstallers.R
\name{greta_remove_all_deps}
\alias{greta_remove_all_deps}
\title{Remove all greta Python dependencies (nuclear reset)}
\usage{
greta_remove_all_deps(ask = interactive())
}
\arguments{
\item{ask}{Ask for confirmation? Default is \code{interactive()}.}
}
\value{
Invisibly, TRUE if anything was removed, otherwise FALSE.
}
\description{
A "nuclear" reset that returns greta to a blank slate. Asks once for
confirmation, it removes:
\enumerate{
\item the 'greta-env-tf2' conda environment, with \code{\link[=remove_greta_env]{remove_greta_env()}}
\item the miniconda installation, with \code{\link[=remove_miniconda]{remove_miniconda()}}
\item reticulate's managed uv cache (if any), with \code{\link[=remove_reticulate_uv_cache]{remove_reticulate_uv_cache()}}
\item greta's stored Python preference (set via \code{\link[=greta_set_python_uv]{greta_set_python_uv()}} and
friends)
}
}
\details{
This is broader than \code{\link[=destroy_greta_deps]{destroy_greta_deps()}}, which only removes the conda
environment and miniconda. Note that a system-wide uv cache is \emph{not} removed
(it is managed by uv itself, and shared beyond reticulate). After running
this, restart R; greta reinstalls what it needs on next use.
}
\examples{
\dontrun{
greta_remove_all_deps()
}
}
\seealso{
\link{reinstallers}, \code{\link[=destroy_greta_deps]{destroy_greta_deps()}}
}
