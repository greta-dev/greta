% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_greta_deps.R
\name{greta_deps_spec}
\alias{greta_deps_spec}
\title{Specify python dependencies for greta}
\usage{
greta_deps_spec(
  tf_version = "2.21.0",
  tfp_version = "0.25.0",
  python_version = "3.12"
)
}
\arguments{
\item{tf_version}{character. TensorFlow version, in the format
major.minor.patch. Default is 2.21.0.}

\item{tfp_version}{Character. Tensorflow probability (TFP) version
major.minor.patch. Default is 0.25.0.}

\item{python_version}{Character. Python version in format major.minor.patch.
Default is 3.12.}
}
\value{
data frame of valid dependencies
}
\description{
A helper function for specifying versions of Tensorflow (TF), Tensorflow
Probability (TFP), and Python. Defaulting to 2.21.0,
0.25.0, and 3.12, respectively.
}
\section{What you can actually choose}{


\strong{TensorFlow, between 2.18.0 and
2.21.0.} This is the one genuine choice. Anything
outside the range is rejected, with the range named in the error. The usual
reason to pick an older version is Python: TensorFlow
2.21.0 has no Python 3.9 wheels.

\strong{Python.} Passed through to the resolver rather than checked by greta.

\strong{TensorFlow Probability: settable, but in practice fixed} at
0.25.0. Older releases pair with TensorFlow versions
greta no longer supports (0.24.0 with 2.16, 0.23.0 with 2.15), so changing
it is unlikely to give you a working environment. greta does not check it,
so an unworkable choice surfaces as a resolver or import error at install
time.
}

\section{Why the range is so narrow}{


TensorFlow Probability sets the floor. Its most recent release is
0.25.0, from November 2024, and that release is tested
against TensorFlow 2.18.0. It \emph{installs} against 2.16
and 2.17 -- its metadata asks only for \code{tensorflow>=2.16}, with no upper
bound -- and then fails to import, which is why greta rejects those versions
up front rather than after a long install.

greta has a floor of its own at TensorFlow 2.16, because its optimisers use
the Keras 3 API and Keras 3 arrives in 2.16. TensorFlow Probability's
requirement is the stricter of the two, so it is the one you meet.

greta installs and runs against this range weekly, on Linux, macOS and
Windows. Each run publishes a table of every combination tried, what each
resolved to, and which did not work; open the most recent from
\url{https://github.com/greta-dev/greta/actions/workflows/install-check.yaml}.

Calling \code{greta_deps_spec()} with no arguments returns greta's current
default (recommended) versions, and is the supported way to query them -
for example \code{greta_deps_spec()$tf_version} for the default TensorFlow
version.
}

\examples{
greta_deps_spec()
# every combination below is one the weekly install check passes on
greta_deps_spec(tf_version = "2.18.0")
greta_deps_spec(tf_version = "2.19.0")
greta_deps_spec(tf_version = "2.20.0")
greta_deps_spec(
  tf_version = "2.19.0",
  tfp_version = "0.25.0",
  python_version = "3.12"
)
# these fail: greta supports TensorFlow 2.18.0 up to the version it pins
\dontrun{
greta_deps_spec(tf_version = "2.17.0")
greta_deps_spec(tf_version = "2.99.0")
}
}
