% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python_backend.R
\name{greta_set_deps}
\alias{greta_set_deps}
\title{Choose the dependency versions greta installs}
\usage{
greta_set_deps(deps = greta_deps_spec())
}
\arguments{
\item{deps}{object created with \code{\link[=greta_deps_spec]{greta_deps_spec()}}, or \code{NULL} to clear
the stored choice. \code{\link[=greta_deps_spec]{greta_deps_spec()}} checks that the TensorFlow version
is one greta supports.}
}
\value{
Invisibly, the stored \code{\link[=greta_deps_spec]{greta_deps_spec()}} (\code{NULL} when clearing).
}
\description{
Persistently choose which versions of TensorFlow, TensorFlow Probability,
and Python greta uses, independently of \emph{where} they are installed (see
\code{\link[=greta_set_python]{greta_set_python()}} for that). The stored versions are used by:
\itemize{
\item the managed (uv) environment, which installs them automatically on first
use after a restart, and
\item \code{\link[=install_greta_deps]{install_greta_deps()}}, as its default \code{deps} argument when building a
conda environment.
}

Most users never need this: greta's defaults (TensorFlow
2.15.1, TensorFlow Probability
0.23.0, Python 3.11) are the
newest versions greta supports. Use \code{greta_set_deps(NULL)} to clear the
stored choice and return to the defaults.
}
\details{
Your choice is stored under \code{tools::R_user_dir("greta", "config")} and
applied the next time greta is loaded, so you will need to \strong{restart R}
for it to take effect. Changing versions on the managed (uv) backend may
require internet access on the next load, to download the newly requested
versions.
}
\examples{
\dontrun{
# pin an older TensorFlow for the managed (uv) environment
greta_set_deps(greta_deps_spec(
  tf_version = "2.14.0",
  tfp_version = "0.22.1",
  python_version = "3.10"
))

# clear the stored choice and return to greta's defaults
greta_set_deps(NULL)
}
}
\seealso{
\code{\link[=greta_set_python]{greta_set_python()}}, \code{\link[=greta_deps_spec]{greta_deps_spec()}}, \code{\link[=install_greta_deps]{install_greta_deps()}}
}
