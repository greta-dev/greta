---
output:
  html_document:
    css: greta.css
    theme: lumen
    highlight: default
    toc: false
---

<style type="text/css">
body{
  padding-top: 30px;
}
pre, pre:not([class]){
  font-size: 20px;
  color: #5a3a78;
  background-color: #f7f7f7;
  border: none;
}
.container-fluid {
    padding-left: 0;
    padding-right: 0;
}
.main-container {
    max-width: 1400px;
    margin-left: 0;
    margin-right: 0;
}
</style>

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, cache = TRUE)
set.seed(1)
```

<div id="head"">
  <p></p>
  <div id="header-content" class="container">
  <br>

```{r logo_title, out.width="50%", fig.align='left'}
knitr::include_graphics('../logos/name_icon_on_light.png')
```
    <p style="color:#ffffff; font-size: 24px;"> &emsp; simple and scalable statistical modelling in R</p>
    
  <br><br>
        
</div>

</div>


<div id="bullets">

<br><br>

<div class="container">
<div class="row">

<div class="col-md-4">
  <p class="bullet"><b>simple</b></p>
  <br>
  greta models are written right in R, so there's no need to learn another language like BUGS or Stan
</div>

<div class="col-md-4">
  <p class="bullet"><b>scalable</b></p>
  <br>
  greta uses [Google TensorFlow](https://www.tensorflow.org/) so it's fast even on massive datasets, and runs on CPU clusters and GPUs
</div>

<div class="col-md-4">
  <p class="bullet"><b>extensible</b></p>
  <br>
  it's easy to write your own R functions, modules, and even packages using greta
</div>

</div>
</div>

<br><br>

</div>

<div id="panel1" class="resource">

<div class="container">

<br>

<div class="row">

<div class="col-md-2"></div>

<div class="col-md-4">
  <span class="fa fa-code fa-2x" style="vertical-align:middle;"></span> &emsp; <a href="#example">basic example</a>
</div>

<div class="col-md-4">
&thinsp; <span class="fa fa-arrow-circle-right fa-2x" style="vertical-align:middle;"></span>&emsp; [getting started](getting_started.html)
</div>

<div class="col-md-2"></div>

</div>

<div class="row">

<div class="col-md-2"></div>

<div class="col-md-4">
  &thinsp; <span class="fa fa-file-code-o fa-2x" style="vertical-align:middle;"></span> &emsp; [example models](example_models.html)
</div>

<div class="col-md-4">
  &thinsp; <span class="fa fa-book fa-2x" style="vertical-align:middle;"></span> &emsp; [package docs](reference-index.html)
</div>

<div class="col-md-2"></div>

</div>

<br>

</div>

</div>



<div id="example">

<div class="container">

<br><br>

<h1 style="color: #8960B3;">basic example</h1>

<div class="row">

<div class="col-md-6">

Here's a Bayesian linear regression model fit to the `iris` data using greta:

```
library(greta)
```
```
x <- as_data(iris$Petal.Length)
y <- as_data(iris$Sepal.Length)

int = normal(0, 5)
coef = normal(0, 3)
sd = lognormal(0, 3)

mean <- int + coef * x
distribution(y) = normal(mean, sd)
```
```
m <- model(int, coef, sd)
```
```{r model, echo = FALSE, eval = TRUE, results='hide', message=FALSE}
library(greta)

x <- as_data(iris$Petal.Length)
y <- as_data(iris$Sepal.Length)

int = normal(0, 5)
coef = normal(0, 3)
sd = lognormal(0, 3)

mean <- int + coef * x
distribution(y) = normal(mean, sd)

m <- model(int, coef, sd)
```
</div>
<div class="col-md-6">
```
draws <- mcmc(m, n_samples = 500)
bayesplot::mcmc_trace(draws)
```
```{r vis, echo = FALSE, eval = TRUE, results='hide', message=FALSE, warning=FALSE, fig.height=4, fig.width=4, fig.align="center"}
draws <- mcmc(m, n_samples = 500)
bayesplot::mcmc_trace(draws, facet_args = list(nrow = 3, ncol = 1))
```

</div>
</div>

<br>

</div>
</div>
