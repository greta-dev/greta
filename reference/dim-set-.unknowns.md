# set dims like on a matrix/array

set dims like on a matrix/array

## Usage

``` r
# S3 method for class 'unknowns'
dim(x) <- value
```

## Arguments

- x:

  matrix/array to set values to

- value:

  values that are being set set
