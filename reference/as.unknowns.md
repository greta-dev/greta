# Create objects of class 'unknowns' to nicely print ? valued arrays

Create objects of class 'unknowns' to nicely print ? valued arrays

## Usage

``` r
as.unknowns(x)
```

## Arguments

- x:

  object to convert to "unknowns" class
